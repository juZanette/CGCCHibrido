set(EXERCISES Hello3D)

foreach(EXERCISE ${EXERCISES})
    add_executable(${EXERCISE} ${EXERCISE}.cpp ${CMAKE_SOURCE_DIR}/common/glad.c)
    target_link_libraries(${EXERCISE} glfw opengl32)
endforeach()
